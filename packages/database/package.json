{
  "name": "@hgbord/database",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "prisma:generate:pg": "prisma generate --schema=./prisma/schema.postgres.prisma",
    "prisma:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "prisma:push": "prisma db push --schema=./prisma/schema.prisma",
    "prisma:push:pg": "prisma db push --schema=./prisma/schema.postgres.prisma",
    "db:seed": "tsx prisma/seed.ts",
    "db:rename-field-ids": "tsx prisma/rename-field-ids.ts",
    "db:migrate:pg": "tsx prisma/migrate-sqlite-to-postgres.ts",
    "db:reset": "prisma migrate reset --schema=./prisma/schema.prisma --force"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.19.43",
    "bcryptjs": "^2.4.3",
    "prisma": "^5.18.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
